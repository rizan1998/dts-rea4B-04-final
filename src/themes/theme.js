import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    type: "dark",
    primary: {
      main: "#282A2B",
    },
    secondary: {
      main: "#FF760D",
    },
  },
});

export default theme;
